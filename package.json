{
	"name": "record_store",
	"private": true,
	"scripts": {
		"dev": "lerna run dev --parallel",
		"dev-backend": "cd backend/app && npm run dev",
		"postinstall": "lerna bootstrap"
	},
	"devDependencies": {
		"lerna": "^4.0.0"
	},
	"dependencies": {
		"slugify": "^1.6.1"
	}
}
